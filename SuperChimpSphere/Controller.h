/*#pragma once
#include <Xinput.h>
#include <unordered_map>
class Controllers
{
	class Controller
	{
	public:
		Controller(unsigned int _index);
		void Tick();
	private:
		unsigned int index;

		std::unordered_map<unsigned int, bool> buttons;
		std::unordered_map<unsigned int, float> axis;
	};
private:
};
*/
